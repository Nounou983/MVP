# Phase 9.7 Round 3 Artifact Trace

This folder is generated by `server/tools/phase97_artifact_trace.py`.

Important: the current bundle did **not** contain the exact runtime mask or raw RORem/LaMa outputs from the user's RTX run. The files named `rorem_raw_seed2026.png`, `rorem_raw_seed11037.png`, and `lama_raw.png` are therefore watermarked placeholders, not model outputs. `mask_submitted.png` is a closest reproducible approximation derived from the UI selection preview and is explicitly marked as such in `trace_summary.json`.

For a real RTX trace, replace these with the exact runtime artifacts and rerun the script. It will compute the mask geometry and raw-vs-final comparisons without guessing missing neural outputs.
