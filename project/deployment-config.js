/* Optional production override. Safe to leave empty for local development.
   Example:
   window.CIGOGNE_CONFIG = {
     apiBase: 'https://YOUR-API.example.com',
     aiMode: 'gateway',
     aiApi: 'https://YOUR-API.example.com/api/ai'
   };
*/
window.CIGOGNE_CONFIG = window.CIGOGNE_CONFIG || {};
